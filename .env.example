# PostgreSQL connection string.
# Local development (docker compose up -d): use the value below as-is.
DATABASE_URL="postgresql://seapedia:seapedia@localhost:5433/seapedia?schema=public"

# Secret used to sign session JWTs. Use a long random string in production.
JWT_SECRET="replace-with-a-long-random-string"
